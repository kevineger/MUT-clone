$('.tab').removeClass('active');
$('#messages-tab').addClass('active');
$('.profile-container-inner').html("<%= escape_javascript(render partial: 'two_pane', locals: {conversations: @conversations, new_message: @message }) %>");
