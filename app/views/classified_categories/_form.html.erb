<%= form_for(@classified_category) do |f| %>
  <% if @classified_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classified_category.errors.count, "error") %> prohibited this classified_category from being saved:</h2>

      <ul>
      <% @classified_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.text_field :name %><br />
    <%= f.text_field :icon %><br />
    <%= f.submit %>
  </div>
<% end %>
